# opencode-free-gate — 前端设计简报

## 项目一句话

AI API 代理网关，通过多代理轮换访问 opencode.ai 的免费模型，实现稳定无限使用。

## 架构

```
客户端 (OpenCode/Cline/任何 OpenAI 兼容客户端)
    ↓  Authorization: Bearer admin123
opencode-free-gate (端口 13339)
    ↓  从 3 个活跃槽位中轮选一个代理
    ↓  Authorization: Bearer public (固定)
代理节点 (socks5/http) → opencode.ai/zen
```

## 核心概念

| 概念 | 说明 |
|:----|:-----|
| **活跃槽位 (Slots)** | 当前正在使用的代理，最多 3~5 个，按轮询分配请求。失败 3 次后自动换一个 |
| **候选池 (Candidates)** | 等待被探活的备用代理，探活成功后晋升为活跃槽位 |
| **代理源 (Sources)** | 定期拉取代理列表的数据源（amux API、GitHub 文本列表等） |
| **WARP** | Cloudflare WARP 代理，作为最后一个兜底节点，始终排在活跃列表末尾 |
| **质量等级** | S > A > B > C，探活时自动评级 |

## 请求流转策略（优先级从高到低）

1. **活跃槽位轮询** → 3 次重试，失败后丢弃该代理
2. **自定义代理兜底**（手动添加的代理列表）

## 后端 API 端点

| 方法 | 路径 | 功能 |
|:-----|:-----|:-----|
| `GET` | `/api/status` | 仪表盘数据：槽位状态、候选数、统计、WARP 状态 |
| `GET` | `/api/proxies` | 所有代理列表（活跃在前且 WARP 永远排最后，候选在后） |
| `POST` | `/api/proxies/promote` | 提拔候选代理替换活跃中延时最高的槽位 |
| `DELETE` | `/api/proxies/:addr` | 删除代理（从所有池中移除） |
| `POST` | `/api/proxies` | 批量添加自定义代理 |
| `GET` | `/api/sources` | 列出所有代理源 |
| `POST` | `/api/sources` | 添加代理源 |
| `DELETE` | `/api/sources/:name` | 删除代理源 |
| `POST` | `/api/sources/refresh` | 刷新所有代理源 |
| `POST` | `/api/slots/fill` | 重新填充活跃槽位 |
| `POST` | `/api/candidates/load` | 重新加载候选池 |
| `POST` | `/api/config` | 读取/更新配置 |
| `GET` | `/api/logs` | 获取运行日志 |

## 聊天请求入口

| 路径 | 说明 |
|:-----|:-----|
| `/openai/v1/chat/completions` | OpenAI 格式（主要） |
| `/openai/v1/models` | 模型列表 |
| `/anthropic/v1/messages` | Anthropic 格式 |

---

## 前端页面结构（5 个 Tab）

### Tab 1: 📊 仪表盘 (Dashboard)

**顶部状态栏：** 运行状态指示灯（绿/红）+ 运行时间

**统计卡片网格（最多 7 张）：**
- 活跃槽位：`2 / 3`（蓝色）
- 候选代理：`2745`（黄色）
- 代理源：`2`
- 总请求：`1234`
- 成功率：`87.5%`（绿/黄/红根据阈值变色）
- 限流次数：`3`（红色）
- WARP 状态：`✅ 运行中` / `❌ 已断开` / `⏸️ 已关闭`

**发送测试请求区域：**
- 模型下拉选择：hy3-free / nemotron-3-ultra-free / deepseek-v4-flash-free / mimo-v2.5-free
- 输入框 + 发送按钮
- 结果显示（成功/失败 + 耗时）

**快速操作按钮：**
- 🔄 刷新代理源
- 🔧 填充槽位
- 📥 加载候选项

### Tab 2: 🔄 代理池 (Proxies)

**顶部操作栏：**
- ➕ 批量添加代理（弹窗，多行文本输入，每行一个 `ip:port` 或 `socks5://ip:port`）
- 🔄 刷新

**代理表格：**

| 地址 | 协议 | 等级 | 延迟 | 状态 | 操作 |
|:-----|:-----|:-----|:-----|:-----|:-----|
| 40.81.205.181:1080 | http | S | 2031ms | 🟢 活跃 | 删除 |
| 68.71.251.134:4145 | socks5 | S | 3260ms | 🟢 活跃 | 删除 |
| host.docker.internal:1080 | socks5 | S | 695ms | 🟢 活跃 (WARP) | 删除 |
| 192.168.1.1:8080 | http | A | 500ms | 🟡 待探活 | 删除 提拔 |

- **活跃代理** 显示绿色 "活跃" 标签 + 延迟（ms）
- **候选代理** 显示黄色 "待探活" 标签 + "提拔" 按钮
- **WARP 节点** 始终排在活跃列表最后
- **提拔功能：** 点击后替换活跃中延时最高的槽位

### Tab 3: 📡 代理源 (Sources)

**代理源表格：**

| 名称 | 类型 | 代理数 | 状态 | 操作 |
|:-----|:-----|:-----|:-----|:-----|
| amux | json | 3284 | 🟢 正常 | 删除 |
| speedx-socks5 | text | 3284 | 🟢 正常 | 删除 |

**添加代理源弹窗：**
- 名称（文本输入）
- URL（文本输入）
- 类型（下拉：文本 / JSON）

### Tab 4: ⚙️ 配置 (Config)

**配置表单：**

| 字段 | 类型 | 说明 |
|:-----|:-----|:-----|
| 端口 | number | 默认 13339 |
| 槽位数 | number | 3~5 |
| 最大重试 | number | 默认 3 |
| 超时 (ms) | number | 默认 120000 |
| 探活超时 (ms) | number | 默认 8000 |
| 刷新间隔 (ms) | number | 默认 300000 |
| 自定义代理 | textarea | 逗号分隔的代理列表 |
| WARP 模式 | select | 开/关 |
| WARP 地址 | text | host:port |

- 💾 保存配置按钮

### Tab 5: 📋 日志 (Logs)

**日志区域：**
- 黑底终端风格，等宽字体
- 自动滚动到底部
- 最多显示最近 200 行
- 🔄 刷新 + 清空 按钮

---

## 视觉风格

- **暗色主题**，背景 `#111`，卡片 `#1a1a2e`
- **主色调** 紫色渐变 `#7c4dff → #448aff`
- **状态色：** 绿色 `#00e676` / 红色 `#ff5252` / 黄色 `#ffd740` / 蓝色 `#448aff`
- 圆角卡片 `12px`，阴影边框
- 响应式布局，移动端适配
- Toast 通知（右下角弹出）
- 模态弹窗（居中，半透明遮罩）

## 数据刷新频率

- 仪表盘：每 3 秒自动刷新
- 代理池：每 10 秒自动刷新
- 日志：手动刷新

## 认证方式

- 前端和 `/api/` 路由：**无需认证**
- 聊天请求（`/v1/chat/completions` 等）：需要 `Authorization: Bearer admin123`
